var PokemonCache = function() {};

PokemonCache.prototype.fetch = function (id) {
  // check if the pokemon is in chache
};

PokemonCache.prototype.save = function (pokemon) {
  // cache the pokemon
};
